version: '3.8'
services:
  db:
    image: postgres:15
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: chat_farmacon
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
